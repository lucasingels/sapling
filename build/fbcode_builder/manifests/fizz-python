[manifest]
name = fizz-python
fbsource_path = fbcode/fizz
shipit_project = fizz
shipit_fbcode_builder = true

[git]
repo_url = https://github.com/facebookincubator/fizz.git

[build]
builder = cmake
subdir = fizz

# Linux-only upstream; enabled on darwin for this fork so fbthrift-python
# (edenfsctl.real's thrift-python runtime) can link against the shared
# folly-python instead of a brew keg built against a different folly.
[build.os=windows]
builder = nop

[cmake.defines]
BUILD_EXAMPLES = OFF

[cmake.defines.os=linux]
CMAKE_POSITION_INDEPENDENT_CODE = ON
BUILD_SHARED_LIBS = ON

# darwin: static PIC archives; they get linked into libthrift_python_cpp.dylib.
[cmake.defines.os=darwin]
CMAKE_POSITION_INDEPENDENT_CODE = ON

[cmake.defines.test=on]
BUILD_TESTS = ON

[cmake.defines.all(os=windows, test=on)]
BUILD_TESTS = OFF

[cmake.defines.test=off]
BUILD_TESTS = OFF

[dependencies]
folly-python
liboqs
libsodium
zlib-python
zstd-python

[dependencies.all(test=on, not(os=windows))]
googletest

[shipit.pathmap]
fbcode/fizz/public_tld = .
fbcode/fizz = fizz
